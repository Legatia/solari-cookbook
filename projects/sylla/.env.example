# Runtime
APP_BASE_URL=http://localhost:3000
INTEGRATION_MODE=mock
# Local convenience only. Public deployments should require /join/<token>.
SYLLA_ENABLE_DEMO_SESSIONS=true

# Server-only credentials
AUTH_SECRET=
DATABASE_URL=
SOLARI_API_KEY=
MODEL_API_KEY=
SYLLA_INTERNAL_MODEL_MODE=mock
# Required only when SYLLA_INTERNAL_MODEL_MODE=live. No model is assumed.
SYLLA_INTERNAL_MODEL=
SYLLA_TRIAL_CREDITS=500

# Vercel sends this bearer secret to the automatic fallback sweep.
CRON_SECRET=
SYLLA_FALLBACK_SWEEP_LIMIT=10

# OAuth resource-server configuration. The external identity provider issues
# JWT access tokens with audience APP_BASE_URL/mcp and scope sylla:agent.
SYLLA_OAUTH_ISSUER=
SYLLA_OAUTH_JWKS_URL=
SYLLA_OAUTH_AUDIENCE=

# Temporary developer MCP bridge. Keep disabled in public deployments.
# Production MCP will replace this with OAuth 2.1 account linking.
SYLLA_ENABLE_DEV_MCP=false
SYLLA_MCP_DEV_TOKEN=
SYLLA_MCP_DEV_PARTICIPANT_ID=

# Solari defaults
SOLARI_BASE_URL=https://api.getsolari.com
